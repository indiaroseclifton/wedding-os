import {ensureDemoWorkspace} from "@/lib/data/workspace";import {getStudio} from "@/lib/data/studio-store";import {ConnectionsHub} from "@/components/studio/ConnectionsHub";export default async function Page({searchParams}:{searchParams:Promise<{project?:string;error?:string}>}){const {workspace}=await ensureDemoWorkspace(),{projects}=await getStudio(workspace.id),s=await searchParams;return <ConnectionsHub projects={projects} initialProject={s.project} initialError={s.error}/>;}
