'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import type {StudioKind} from '@/lib/studio-project';import {KIND_LABELS,uid} from '@/lib/studio/design';import {Field,FileButton,NumberField,saveBody,studioRequest,uploadStudioFile} from './StudioUI';
export function StudioIntake(){const router=useRouter(),[title,setTitle]=useState(''),[kind,setKind]=useState<StudioKind>('floral'),[qty,setQty]=useState(1),[photo,setPhoto]=useState(''),[url,setUrl]=useState(''),[note,setNote]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function create(){setBusy(true);setError('');try{if(url&&!/^https:\/\//i.test(url))throw new Error('Use a full HTTPS inspiration link.');const{project}=await studioRequest({action:'create',kind,qty,title:title.trim()||undefined}),d=project.design!;if(photo)d.references.push({id:uid(),title:title||'My inspiration',url:photo,imageUrl:photo,note,source:'upload'});if(url)d.references.push({id:uid(),title:'Original inspiration',url,note,source:url.includes('pinterest')||url.includes('pin.it')?'pinterest':'link'});await studioRequest(saveBody({...project,note},d));router.push(`/studio/projects/${project.id}`);}catch(e){setError((e as Error).message);setBusy(false);}}return <div className="studio-intake"><header className="st-page-heading"><div><p className="st-eyebrow">Start with something you love</p><h1>Let’s make that.</h1><p>A photograph, a saved Pin, or an idea that won’t leave you alone. Give it a place to become real.</p></div></header><form className="st-intake-grid" onSubmit={e=>{e.preventDefault();void create();}}><div className="st-intake-photo">{photo?<img src={photo} alt="Your uploaded inspiration"/>:<div><span>＋</span><h2>Bring the look.</h2><p>Keep a real reference beside your design and share it with the Studio assistant.</p></div>}<FileButton disabled={busy} accept="image/png,image/jpeg,image/webp" onFile={async f=>{setBusy(true);try{const file=await uploadStudioFile(f);setPhoto(file.url);}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}>{photo?'Replace reference':'Choose a reference photo'}</FileButton></div><div><Field label="What are you making?"><select value={kind} onChange={e=>setKind(e.target.value as StudioKind)}>{Object.entries(KIND_LABELS).map(([id,label])=><option key={id} value={id}>{label}</option>)}</select></Field><Field label="Project name"><input value={title} maxLength={150} placeholder="Our garden centerpieces" onChange={e=>setTitle(e.target.value)}/></Field><NumberField label="How many finished pieces?" value={qty} min={1} max={10000} onChange={n=>setQty(Math.round(n))}/><Field label="Pinterest or inspiration link · optional"><input type="url" value={url} placeholder="https://" onChange={e=>setUrl(e.target.value)}/></Field><Field label="What do you love about this look?"><textarea value={note} maxLength={2000} onChange={e=>setNote(e.target.value)}/></Field><p className="st-help">You’ll begin with editable materials. AI can help interpret an uploaded photo from inside the project; you review the recipe before making.</p>{error&&<p className="st-error" role="alert">{error}</p>}<button className="st-button st-primary" disabled={busy}>{busy?'Preparing…':'Open my workbench →'}</button></div></form></div>;}
